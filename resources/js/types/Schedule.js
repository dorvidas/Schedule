export default class Schedule {
    constructor(date, timeFrom, timeTo, name) {
        this.date = date;
        this.timeFrom = timeFrom;
        this.timeTo = timeTo;
        this.name = name;
    }
}
